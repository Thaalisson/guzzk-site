/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // Permite o uso do App Router corretamente
    },
  };
  
  export default nextConfig;
  